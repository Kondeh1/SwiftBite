<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delivery Dispatch | SwiftBite</title>
    <link rel="stylesheet" href="/css/staff.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .delivery-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 20px; margin-top: 20px; }
        .delivery-card { background: white; border-radius: 12px; padding: 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); border: 1px solid #e2e8f0; position: relative; }
        .code-badge { background: #fef3c7; color: #92400e; padding: 10px; border-radius: 8px; border: 1px dashed #f59e0b; text-align: center; margin: 15px 0; }
        .code-badge small { display: block; font-size: 0.7rem; text-transform: uppercase; margin-bottom: 2px; }
        .code-badge strong { font-size: 1.4rem; letter-spacing: 2px; }
        .status-waiting { color: #64748b; font-size: 0.85rem; display: flex; align-items: center; gap: 8px; justify-content: center; margin-top: 10px; }
        .btn-simulate { width: 100%; background: #1e293b; color: white; border: none; padding: 10px; border-radius: 6px; cursor: pointer; font-size: 0.8rem; margin-top: 10px; }
    </style>
</head>
<body style="background: #f8fafc;">
    <%- include("../partials/header.ejs") %>
    <div class="dashboard-container">
        <%- include("../partials/asideStaff.ejs") %>

        <main class="admin-main">
            <div class="header">
                <h1><i class="fa-solid fa-truck-ramp-box" style="color: #ff4f18;"></i> Active Deliveries</h1>
                <p>Orders awaiting customer verification code.</p>
            </div>

            <% if (orders && orders.length > 0) { %>
           <div class="delivery-grid">
    <% orders.forEach(order => { %>
        <div class="delivery-card" style="border-left: 5px solid #3b82f6;">
            <div class="card-header">
                <span class="order-id">#<%= order.id %></span>
                <span class="badge-transit"><i class="fa-solid fa-truck-fast"></i> IN TRANSIT</span>
            </div>

            <div class="customer-info">
                <h3 style="margin: 0;"><%= order.customer_name %></h3>
                <p style="color: #64748b; font-size: 0.9rem;"><%= order.customer_address %></p>
                <a href="tel:<%= order.customer_phone %>" class="phone-link">
                    <i class="fa-solid fa-phone"></i> <%= order.customer_phone %>
                </a>
            </div>

            <div style="background: #f8fafc; border: 2px dashed #cbd5e1; padding: 15px; border-radius: 10px; margin-top: 15px; text-align: center;">
                <span style="display: block; font-size: 0.7rem; color: #64748b; font-weight: 700; text-transform: uppercase;">Provide this code to Customer:</span>
                <span style="font-family: monospace; font-size: 1.8rem; font-weight: 800; color: #1e293b; letter-spacing: 4px;">
                    <%= order.verification_code %>
                </span>
            </div>

            <div style="margin-top: 15px; text-align: center; color: #059669; font-weight: 600; font-size: 0.85rem;">
                <i class="fa-solid fa-circle-notch fa-spin"></i> Awaiting Customer Confirmation
            </div>
        </div>
    <% }) %>
</div>
            <% } else { %>
                <div style="text-align: center; padding: 80px; background: white; border-radius: 12px; margin-top: 20px;">
                    <i class="fa-solid fa-box-open" style="font-size: 3rem; color: #cbd5e1; margin-bottom: 10px;"></i>
                    <p style="color: #64748b;">No deliveries currently in transit.</p>
                </div>
            <% } %>
        </main>
    </div>

    <script>
        async function simulateCustomer(orderId) {
            const inputCode = prompt("CUSTOMER SIMULATION\nPlease enter the 6-digit verification code:");
            
            if (!inputCode) return;

            try {
                const response = await fetch('/customer/verify-delivery', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ orderId, inputCode })
                });

                const data = await response.json();

                if (data.success) {
                    alert("Success! Order status updated to 'completed'.");
                    document.getElementById(`card-${orderId}`).style.display = 'none';
                    location.reload();
                } else {
                    alert("Error: " + data.message);
                }
            } catch (err) {
                console.error(err);
                alert("Request failed.");
            }
        }
    </script>
</body>
</html>