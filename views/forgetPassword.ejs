<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Forgot Password</title>
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body>
    <div class="role-page"> 
      <h2>Reset Your Password</h2>
      <p>Enter your email and we'll send you a link to reset your password.</p>

      <% if (locals.message) { %>
          <div class="alert error" style="color: var(--sb-orange); margin-bottom: 10px; font-weight: bold;">
            <%= message %>
          </div>
      <% } %>

      <form action="/forgot-password" method="POST" class="horizontal-form">
          <input type="email" name="email" placeholder="Enter your email" required>
          <button type="submit" class="btnc">Verify & Send Link</button>
      </form>

      <div style="margin-top: 20px;">
        <a href="/login" style="color: var(--sb-orange); text-decoration: none; font-weight: 600;">Back to Login</a>
      </div>
    </div>
  </body>
</html>